<Window x:Class="np_project.Views.LoginWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:np_project.Views"
      mc:Ignorable="d"
      Title="Login" Height="auto" Width="auto" WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="DNI"/>
            <TextBox Text="{Binding Dni, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,10"/>

            <TextBlock Text="Contraseña"/>
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,4,0,10"/>
            
            <Button Content="Iniciar Sesión" Command="{Binding LoginCommand}" Height="32"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
