<UserControl x:Class="np_project.Views.Home"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:np_project.Views"
        mc:Ignorable="d"
        Background="#F1F1F1"
        Height="auto" Width="auto">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">

            <Border Grid.Column="1" 
                    Background="#F1F1F1" 
                    Margin="10">
                <Image Source="/Resources/Images/DashBoard.png" 
                   Width="Auto" 
                   Height="Auto" 
                   Margin="0,0,8,0"/>
            </Border>
        </Grid>

        <Border
            Grid.Row="0"
            Background="White"
            Height="30"
            CornerRadius="0,0,10,10">

            <StackPanel Orientation="Horizontal">
                <Image Source="/Resources/Images/NP.png" HorizontalAlignment="Left" Margin="17,0,0,0"/>
                <TextBlock Text="NP - Projects" Foreground="Black" VerticalAlignment="Center"  FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
